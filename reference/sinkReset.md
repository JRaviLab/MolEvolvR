# Sink Reset

Sink Reset

## Usage

``` r
sinkReset()
```

## Value

No return, but run to close all outstanding
[`sink()`](https://rdrr.io/r/base/sink.html)s

## Examples

``` r
if (FALSE) { # \dontrun{
sinkReset()
} # }
```
