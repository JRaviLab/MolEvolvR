# extractAccNum

extractAccNum

## Usage

``` r
extractAccNum(string)
```

## Arguments

- string:

  A string from which to extract the accession number. The string may
  contain accession information delimited by `|` or spaces.

## Value

Describe return, in detail

## Examples

``` r
if (FALSE) { # \dontrun{
extractAccNum()
} # }
```
