# validateCountDF

validateCountDF

## Usage

``` r
validateCountDF(var)
```

## Arguments

- var:

  A data frame whose columns are to be converted.

## Value

A data frame with non-'n' columns converted to character type.

## Examples

``` r
if (FALSE) { # \dontrun{
new_df <- .all_non_n_cols_to_char(my_data)
} # }
```
