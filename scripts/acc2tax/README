acc2tax takes in accessions as input finds the corresponding mapping to taxid

****************Compiling****************
Enter 'make' in the commandline while in this directory,
and the program should be compiled.

****************USAGE********************

./acc2tax -f mappingfile -o outputfile -l logfile inputfile

-f
This is a required argument for the path to the file that maps accessions to
taxids.

-o 
This is a required argument for the path to the outputfile where the curated list
of accessions to taxids will be stored.

-l 
This is an optional argument of where the logfile will be stored. Defaults to 
storing it in the program directory as 'acc2tax.log'.

Any other commandline arguments provided will be interpreted as inputfiles
containing whitespace delimited accessions.